// import { employees } from 'db/schema';

// export const createEmployee = async (_: any, args: any, { db }: any) => {
//   const result = await db.insert(employees).values(args).returning();
//   return result[0];
// };
